node_modules/
.idea/
.vercel/
.env
.env.local
*.log
*.bak
.DS_Store

# Never commit the Firebase service-account JSON. Anyone holding it has full
# read/write on the whole database, bypassing every security rule.
*serviceAccount*.json
firebase-adminsdk-*.json

# GitHub Pages artifact. The site is on Vercel now, and a committed CNAME
# re-registers do2date.com as a Pages custom domain on every push.
CNAME
